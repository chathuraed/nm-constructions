import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "./components/Header";
import Footer from "./components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "NM Constructions & Landscaping - Building Strong Homes, Designing Beautiful Landscapes",
  description: "Professional construction and landscaping services in Sri Lanka. Residential & commercial construction, hotel landscaping, swimming pools, and garden design.",
  keywords: "construction company Sri Lanka, house builders, hotel landscaping, swimming pool construction, pond construction, commercial building contractors",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Header />
        {children}
        <Footer />
      </body>
    </html>
  );
}
